apiVersion: v1
kind: Service
metadata:
  name: plant-disease-api-service
spec:
  type: NodePort
  selector:
    app: plant-disease-api
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 30080